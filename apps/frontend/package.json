{
  "name": "airops-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/",
    "start": "npx serve src"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
