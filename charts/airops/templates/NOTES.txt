AirOps lab installed.

Frontend:
  http://127.0.0.1:{{ .Values.caddy.service.nodePort }}
  http://<node-ip>:{{ .Values.caddy.service.nodePort }}
{{- if .Values.ingress.enabled }}
  http://{{ .Values.ingress.host }}
{{- range .Values.ingress.extraHosts }}
  http://{{ . }}
{{- end }}
{{- end }}

GraphQL:
  http://127.0.0.1:{{ .Values.caddy.service.nodePort }}/graphql
  http://<node-ip>:{{ .Values.caddy.service.nodePort }}/graphql
{{- if .Values.ingress.enabled }}
  http://{{ .Values.ingress.host }}/graphql
{{- range .Values.ingress.extraHosts }}
  http://{{ . }}/graphql
{{- end }}
{{- end }}

Useful commands:
  kubectl -n {{ .Release.Namespace }} get pods
  kubectl -n {{ .Release.Namespace }} logs deploy/{{ include "airops.fullname" . }}-api -f
  kubectl -n {{ .Release.Namespace }} get events --sort-by=.lastTimestamp
